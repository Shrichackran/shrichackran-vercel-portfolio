{
  "name": "shrichackran-3d-interactive-portfolio-complete",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/ && cp -r css js assets dist/ && cp README.md dist/ || true"
  }
}
